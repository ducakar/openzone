if( OZ_BUILD_CLIENT )
  add_executable( openzone openzone.cpp )
  add_dependencies( openzone client ui nirvana matrix pch )
  target_link_libraries( openzone client ui nirvana matrix oz ${libs} )
endif( OZ_BUILD_CLIENT )

if( OZ_BUILD_TOOLS )
  add_executable( ozPrebuildData ozPrebuildData.cpp )
  add_dependencies( ozPrebuildData client ui nirvana matrix pch )
  target_link_libraries( ozPrebuildData client ui nirvana matrix oz ${libs} )
endif( OZ_BUILD_TOOLS )
