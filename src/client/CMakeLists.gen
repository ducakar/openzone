#!/bin/sh

cat << EOF > CMakeLists.txt
add_executable( openzone
EOF

ls *.cpp >> CMakeLists.txt

cat << EOF >> CMakeLists.txt
)

add_dependencies( openzone precompiled )
target_link_libraries( openzone ui nirvana matrix base )
EOF
