#!/bin/sh

cat << EOF > CMakeLists.txt
add_executable( server
EOF

ls *.cpp >> CMakeLists.txt

cat << EOF >> CMakeLists.txt
)

add_dependencies( server precompiled )
target_link_libraries( server nirvana matrix base )
EOF
