#pragma once

// prebuild resources 
#cmakedefine OZ_MAKE_PREBUILT

// whether to use prebuilt resources
#cmakedefine OZ_USE_PREBUILT

// cylinder collision and physics support
#cmakedefine OZ_ENABLE_CYLINDER

// path to default data directory
#define OZ_DEFAULT_DATA_DIR "@OZ_DEFAULT_DATA_DIR@"

// fix error in vorbisfile.h
#ifdef OZ_MINGW
# define OV_EXCLUDE_STATIC_CALLBACKS
#endif 
