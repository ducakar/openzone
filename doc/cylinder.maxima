/*
  test za mimobežnost
  || rel0 x s || > R

  čas zadetka
  || rel0 + s*t ||^2 == R^2

  v 2D:
*/

e1: (rx + sx*t)^2 + (ry + sy*t)^2 = R^2;

factor( solve(e1, t) );

factor( rx^2 * sx^2 - 2*rx*ry*sx*sy + ry^2 * sy^2 );
